import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.urbanstyle.app',
  appName: 'UrbanStyle AR',
  webDir: 'dist/frontend-angular-ar/browser'
};

export default config;
